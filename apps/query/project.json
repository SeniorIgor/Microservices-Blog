{
  "name": "query",
  "root": "apps/query",
  "sourceRoot": "apps/query/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run build --workspace @org/query"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run dev --workspace @org/query"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run start --workspace @org/query"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run lint --workspace @org/query"
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run type-check --workspace @org/query"
      }
    }
  },
  "tags": ["scope:backend", "service:query"]
}